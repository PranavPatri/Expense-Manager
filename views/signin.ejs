<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Expense Manager</title>

    <link rel="stylesheet" href="signin.css">
    
    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/a9375f4385.js" crossorigin="anonymous"></script>

    <!-- Bootstrap cdn -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
</head>
<body class="text-center">
    <div id="parent">
        <form class="form-signin" action="/signin" method="post">
            
            <h1 class="h3 mb-3 font-weight-normal">Welcome back!</h1>
            <h2 class="h3 mb-3 font-weight-normal">Login to your Account</h2>
            <% if(!userExists) { %>
                <small>That was an invalid username or password. Try again!</small> <br>
            <% } %>

            <div class="main-fields">
                <label>Username</label>
                <div class="input-group mb-3 <%= !userExists ? 'error-border' : '' %>">
                    <div class="input-group-prepend">
                        <span class="input-group-text" style="height: 51px; border-bottom-right-radius: 0; border-top-right-radius: 0;">
                            <i class="fa-solid fa-user fa-lg"></i>
                        </span>
                    </div>
                    <input type="text" name="name" class="form-control" required autofocus>
                </div>
    
                <label>Password</label>
                <div class="input-group mb-3 <%= !userExists ? 'error-border' : '' %>">
                    <div class="input-group-prepend">
                        <span class="input-group-text" style="height: 51px; border-bottom-right-radius: 0; border-top-right-radius: 0;">
                            <i class="fa-solid fa-lock fa-lg"></i>
                        </span>
                    </div>
                    <input type="password" name="pwd" id="password" class="form-control" required>
                </div>
                <br>
                <button id="signupBtn" class="btn btn-lg btn-warning btn-block" type="submit">Sign in</button>
            </div>
        </form>
    </div> <br>
    <p>Don't have an account? <a href="/signup">Sign up and get started!</a></p>
    <p class="mt-5 mb-3 text-muted">&copy; Expense Manager</p>

</body>

<!-- 
<body>
    <section id="signin">
        <div>
            <h1>Login</h1> <br>
            <form action="/signin" method="post">
                <label for="username">Username</label> <br>
                <input id="username" type="text" name="name" required autofocus> <br>
                <% if(!userExists) { %>
                    <small style="visibility: visible;">User doesn't exist</small> <br>
                <% }else %>
                    <small>User doesn't exist</small> <br>
                <input class="submit" type="submit">
            </form> <br>
            <p>Don't have account? <a href="/signup">Create</a></p>
        </div>
    </section>
</body> -->
</html>